version: "3.8"
networks:
  backend:

services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./:/app:delegated
      - /app/node_modules
    tty: true
#    command: "yarn build && yarn export"
