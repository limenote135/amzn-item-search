rules_version = '2';
service firebase.storage {
  function onlyIdentityUser(uid) {
    return request.auth.uid == uid;
  }

  match /b/{bucket}/o {
    match /Users/{uid}/{allPaths=**} {
      allow write: if onlyIdentityUser(uid);
    }

    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}